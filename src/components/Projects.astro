---
const projects = [
  {
    title: "E-Commerce Platform",
    description: "A full-featured online store with cart functionality, user authentication, and payment processing integration.",
    tags: ["React", "Node.js", "MongoDB", "Stripe"],
    link: "#",
    image: "bg-blue-900" // placeholder class
  },
  {
    title: "Task Management App",
    description: "A collaborative productivity tool allowing teams to organize tasks, track progress, and communicate in real-time.",
    tags: ["Vue.js", "Firebase", "Tailwind CSS"],
    link: "#",
    image: "bg-purple-900" // placeholder class
  },
  {
    title: "Weather Dashboard",
    description: "A beautiful weather application providing detailed forecasts and historical data visualization.",
    tags: ["Next.js", "OpenWeather API", "Chart.js"],
    link: "#",
    image: "bg-teal-900" // placeholder class
  }
];
---

<section id="projects" class="py-20 bg-slate-900">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="lg:text-center mb-16">
      <h2 class="text-base text-teal-400 font-semibold tracking-wide uppercase">Portfolio</h2>
      <p class="mt-2 text-3xl leading-8 font-extrabold tracking-tight text-white sm:text-4xl">
        Featured Projects
      </p>
      <p class="mt-4 max-w-2xl text-xl text-slate-400 lg:mx-auto">
        A selection of my recent work demonstrating my technical capabilities and design sensibilities.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {projects.map((project) => (
        <div class="bg-slate-800 rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 group">
          <div class={`h-48 ${project.image} flex items-center justify-center`}>
            <span class="text-white/50 font-bold text-xl">{project.title} Preview</span>
          </div>
          <div class="p-6">
            <h3 class="text-xl font-bold text-white mb-2 group-hover:text-teal-400 transition-colors">{project.title}</h3>
            <p class="text-slate-400 mb-4 h-20 overflow-hidden">{project.description}</p>
            <div class="flex flex-wrap gap-2 mb-6">
              {project.tags.map((tag) => (
                <span class="px-3 py-1 bg-slate-700 text-teal-300 text-xs font-medium rounded-full">
                  {tag}
                </span>
              ))}
            </div>
            <a href={project.link} class="inline-flex items-center text-teal-400 hover:text-teal-300 font-medium">
              View Project
              <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
              </svg>
            </a>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
